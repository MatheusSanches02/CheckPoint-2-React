import styled from 'styled-components';

export const ElHtml = styled.html`
    footer{
        background-color: var(--primary);
        color: var(--white);
        font-family: 'Regular';
        width: 100%
        font-size: 32em;
        min-width: 48em
    }
    
`